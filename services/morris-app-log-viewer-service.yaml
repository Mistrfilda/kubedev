apiVersion: v1
kind: Service
metadata:
    name: morris-app-log-viewer
spec:
    type: NodePort
    ports:
        - name: morris-app
          protocol: TCP
          port: 3000
          targetPort: 3000
    selector:
        app: morris-app
