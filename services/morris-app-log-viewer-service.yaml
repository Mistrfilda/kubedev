apiVersion: v1
kind: Service
metadata:
    name: morris-app-log-viewer
    namespace: morris
spec:
    selector:
        app: morris-app
    ports:
        - protocol: TCP
          port: 3000
          targetPort: 3000
