apiVersion: v1
kind: Service
metadata:
    name: gotenberg
    namespace: gotenberg
    labels:
        app: gotenberg
spec:
    type: ClusterIP
    ports:
        - name: http
          port: 3000
          targetPort: http
          protocol: TCP
    selector:
        app: gotenberg
